# Guidelines

This file used to be a Figma Make template. Project rules now live in:

- [`README.md`](../README.md) — architecture, API, trust model, file map
- [`AGENTS.md`](../AGENTS.md) — short non-negotiables for coding agents

When changing UI, keep the dark floating identity (`bg-slate-950`, cyan→violet primary actions, visible `focus:ring-2 focus:ring-cyan-500`). Prefer flex/grid over absolute positioning except for canvas overlays that must float on the board.
